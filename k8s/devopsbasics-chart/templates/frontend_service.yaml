apiVersion: v1
kind: Service
metadata:
  name: {{ include "devopsbasics-chart.fullname" . }}
  labels:
    {{- include "devopsbasics-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devopsbasics-chart.selectorLabels" . | nindent 4 }}
