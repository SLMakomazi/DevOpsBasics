apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devopsbasics-chart.fullname" . }}-test-connection"
  labels:
    {{- include "devopsbasics-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devopsbasics-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
